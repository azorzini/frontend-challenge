import React from 'react';
import { ResponsiveDrawer } from '../components';

export default {
  title: 'ResponsiveDrawer',
  component: ResponsiveDrawer,
};

export const simple = () => <ResponsiveDrawer />;
